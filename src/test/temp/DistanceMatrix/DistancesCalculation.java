package pl.poznan.put.DistanceMatrix;

import pl.poznan.put.RnaMatrix.MatrixCalculable;
import pl.poznan.put.RnaMatrix.RnaMatrix;
import pl.poznan.put.pdb.analysis.PdbModel;

public class DistancesCalculation implements MatrixCalculable {

    /**
     * @param model
     * @return some derivative matrix.
     */
    @Override
    public RnaMatrix calculateMatrix(PdbModel model) {
        return null;
    }
}
